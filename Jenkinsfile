pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/example/repository.git', branch: 'master'
            }
        }
    }
}

